import React, { memo } from 'react';
import { View, Text } from 'react-native';
import styles from './styles';

const UpdateSection = props => {
  return (
    <View>
      <Text>Hello new conmpoent</Text>
    </View>
  );
};

export default memo(UpdateSection);
